diff --git a/include/openssl/prefix_symbols.h b/include/openssl/prefix_symbols.h
index e60131a6a..574c0a76c 100644
--- a/include/openssl/prefix_symbols.h
+++ b/include/openssl/prefix_symbols.h
@@ -2371,6 +2371,9 @@
 #pragma redefine_extname SSL_set_chain_and_key BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_chain_and_key)
 #pragma redefine_extname SSL_set_cipher_list BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_cipher_list)
 #pragma redefine_extname SSL_set_client_CA_list BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_client_CA_list)
+#pragma redefine_extname SSL_set_grease_enabled BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_grease_enabled)
+#pragma redefine_extname SSL_set_client_hello_padding_enabled BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_client_hello_padding_enabled)
+#pragma redefine_extname SSL_set_client_hello_padding_length BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_client_hello_padding_length)
 #pragma redefine_extname SSL_set_compliance_policy BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_compliance_policy)
 #pragma redefine_extname SSL_set_connect_state BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_connect_state)
 #pragma redefine_extname SSL_set_custom_verify BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_custom_verify)
@@ -2393,6 +2396,7 @@
 #pragma redefine_extname SSL_set_ocsp_response BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_ocsp_response)
 #pragma redefine_extname SSL_set_options BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_options)
 #pragma redefine_extname SSL_set_permute_extensions BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_permute_extensions)
+#pragma redefine_extname SSL_set_grease_sigalgs_enabled BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_grease_sigalgs_enabled)
 #pragma redefine_extname SSL_set_private_key_method BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_private_key_method)
 #pragma redefine_extname SSL_set_psk_client_callback BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_psk_client_callback)
 #pragma redefine_extname SSL_set_psk_server_callback BORINGSSL_ADD_USER_LABEL_AND_PREFIX(SSL_set_psk_server_callback)
@@ -5512,6 +5516,9 @@
 #define SSL_set_compliance_policy BORINGSSL_ADD_PREFIX(SSL_set_compliance_policy)
 #define SSL_set_connect_state BORINGSSL_ADD_PREFIX(SSL_set_connect_state)
 #define SSL_set_custom_verify BORINGSSL_ADD_PREFIX(SSL_set_custom_verify)
+#define SSL_set_grease_enabled BORINGSSL_ADD_PREFIX(SSL_set_grease_enabled)
+#define SSL_set_client_hello_padding_enabled BORINGSSL_ADD_PREFIX(SSL_set_client_hello_padding_enabled)
+#define SSL_set_client_hello_padding_length BORINGSSL_ADD_PREFIX(SSL_set_client_hello_padding_length)
 #define SSL_set_early_data_enabled BORINGSSL_ADD_PREFIX(SSL_set_early_data_enabled)
 #define SSL_set_enable_ech_grease BORINGSSL_ADD_PREFIX(SSL_set_enable_ech_grease)
 #define SSL_set_ex_data BORINGSSL_ADD_PREFIX(SSL_set_ex_data)
@@ -5531,6 +5538,7 @@
 #define SSL_set_ocsp_response BORINGSSL_ADD_PREFIX(SSL_set_ocsp_response)
 #define SSL_set_options BORINGSSL_ADD_PREFIX(SSL_set_options)
 #define SSL_set_permute_extensions BORINGSSL_ADD_PREFIX(SSL_set_permute_extensions)
+#define SSL_set_grease_sigalgs_enabled BORINGSSL_ADD_PREFIX(SSL_set_grease_sigalgs_enabled)
 #define SSL_set_private_key_method BORINGSSL_ADD_PREFIX(SSL_set_private_key_method)
 #define SSL_set_psk_client_callback BORINGSSL_ADD_PREFIX(SSL_set_psk_client_callback)
 #define SSL_set_psk_server_callback BORINGSSL_ADD_PREFIX(SSL_set_psk_server_callback)
diff --git a/include/openssl/ssl.h b/include/openssl/ssl.h
index 3b48c2a08..5b4295a7c 100644
--- a/include/openssl/ssl.h
+++ b/include/openssl/ssl.h
@@ -2617,6 +2617,7 @@ OPENSSL_EXPORT size_t SSL_CTX_get_num_tickets(const SSL_CTX *ctx);
 #define SSL_GROUP_SECP384R1 24
 #define SSL_GROUP_SECP521R1 25
 #define SSL_GROUP_X25519 29
+#define SSL_GROUP_X25519_KYBER768 0x6399
 #define SSL_GROUP_X25519_MLKEM768 0x11ec
 #define SSL_GROUP_MLKEM1024 0x0202

@@ -5551,6 +5552,9 @@ OPENSSL_EXPORT void SSL_CTX_set_retain_only_sha256_of_client_certs(SSL_CTX *ctx,
 // GREASE. See RFC 8701.
 OPENSSL_EXPORT void SSL_CTX_set_grease_enabled(SSL_CTX *ctx, int enabled);

+// SSL_set_grease_enabled overrides GREASE for one connection.
+OPENSSL_EXPORT void SSL_set_grease_enabled(SSL *ssl, int enabled);
+
 // SSL_CTX_set_grease_sigalgs_enabled configures whether sockets on `ctx` should
 // send a GREASE value in signature_algorithms extensions in ClientHello
 // messages. See RFC 8701.
@@ -5561,6 +5565,17 @@ OPENSSL_EXPORT void SSL_CTX_set_grease_enabled(SSL_CTX *ctx, int enabled);
 OPENSSL_EXPORT void SSL_CTX_set_grease_sigalgs_enabled(SSL_CTX *ctx,
                                                        int enabled);

+// SSL_set_grease_sigalgs_enabled configures GREASE signature algorithms for
+// one connection, overriding the SSL_CTX value copied by SSL_new.
+OPENSSL_EXPORT void SSL_set_grease_sigalgs_enabled(SSL *ssl, int enabled);
+
+// SSL_set_client_hello_padding_enabled configures compatibility padding for
+// one connection. It does not affect QUIC ClientHellos.
+OPENSSL_EXPORT void SSL_set_client_hello_padding_enabled(SSL *ssl,
+                                                         int enabled);
+OPENSSL_EXPORT void SSL_set_client_hello_padding_length(SSL *ssl,
+                                                        uint16_t length);
+
 // SSL_CTX_set_permute_extensions configures whether sockets on `ctx` should
 // permute extensions. For now, this is only implemented for the ClientHello.
 OPENSSL_EXPORT void SSL_CTX_set_permute_extensions(SSL_CTX *ctx, int enabled);
diff --git a/ssl/extensions.cc b/ssl/extensions.cc
index 954af185b..547f0e298 100644
--- a/ssl/extensions.cc
+++ b/ssl/extensions.cc
@@ -107,7 +107,8 @@ static bool tls1_check_duplicate_extensions(const CBS *cbs) {
 }

 static bool is_post_quantum_group(uint16_t id) {
-  return id == SSL_GROUP_X25519_MLKEM768 || id == SSL_GROUP_MLKEM1024;
+  return id == SSL_GROUP_X25519_KYBER768 ||
+         id == SSL_GROUP_X25519_MLKEM768 || id == SSL_GROUP_MLKEM1024;
 }

 bool ssl_parse_client_hello_with_trailing_data(const SSLImpl *ssl, CBS *cbs,
@@ -1029,7 +1030,7 @@ static bool ext_sigalgs_add_clienthello(const SSL_HANDSHAKE *hs, CBB *out,
     return false;
   }
   // Add a fake signature algorithm. See RFC 8701.
-  if (hs->ssl->ctx->grease_sigalgs_enabled &&
+  if (hs->config->grease_sigalgs_enabled &&
       !CBB_add_u16(&sigalgs_cbb,
                    ssl_get_grease_value(hs, ssl_grease_signature_algorithm))) {
     return false;
@@ -2468,7 +2469,7 @@ bool ssl_setup_key_shares(SSL_HANDSHAKE *hs, uint16_t override_group_id) {
     return false;
   }

-  if (override_group_id == 0 && ssl->ctx->grease_enabled) {
+  if (override_group_id == 0 && ssl->config->grease_enabled) {
     // Add a fake group. See RFC 8701.
     if (!CBB_add_u16(cbb.get(), ssl_get_grease_value(hs, ssl_grease_group)) ||
         !CBB_add_u16(cbb.get(), 1 /* length */) ||
@@ -2675,7 +2676,7 @@ static bool ext_supported_versions_add_clienthello(
   }

   // Add a fake version. See RFC 8701.
-  if (ssl->ctx->grease_enabled &&
+  if (ssl->config->grease_enabled &&
       !CBB_add_u16(&versions, ssl_get_grease_value(hs, ssl_grease_version))) {
     return false;
   }
@@ -2739,7 +2740,7 @@ static bool ext_supported_groups_add_clienthello(const SSL_HANDSHAKE *hs,
   }

   // Add a fake group. See RFC 8701.
-  if (ssl->ctx->grease_enabled &&
+  if (ssl->config->grease_enabled &&
       !CBB_add_u16(&groups_bytes, ssl_get_grease_value(hs, ssl_grease_group))) {
     return false;
   }
@@ -4391,7 +4392,7 @@ static bool ssl_add_clienthello_tlsext_inner(SSL_HANDSHAKE *hs, CBB *out,

   hs->inner_extensions_sent = 0;

-  if (ssl->ctx->grease_enabled) {
+  if (ssl->config->grease_enabled) {
     // Add a fake empty extension. See RFC 8701. This always matches
     // `ssl_add_clienthello_tlsext`, so compress it.
     uint16_t grease_ext = ssl_get_grease_value(hs, ssl_grease_extension1);
@@ -4429,7 +4430,7 @@ static bool ssl_add_clienthello_tlsext_inner(SSL_HANDSHAKE *hs, CBB *out,
     }
   }

-  if (ssl->ctx->grease_enabled) {
+  if (ssl->config->grease_enabled) {
     // Add a fake non-empty extension. See RFC 8701. This always matches
     // `ssl_add_clienthello_tlsext`, so compress it.
     uint16_t grease_ext = ssl_get_grease_value(hs, ssl_grease_extension2);
@@ -4504,7 +4505,7 @@ bool ssl_add_clienthello_tlsext(SSL_HANDSHAKE *hs, CBB *out, CBB *out_encoded,
   hs->extensions.sent = 0;

   // Add a fake empty extension. See RFC 8701.
-  if (ssl->ctx->grease_enabled &&
+  if (ssl->config->grease_enabled &&
       !add_padding_extension(
           &extensions, ssl_get_grease_value(hs, ssl_grease_extension1), 0)) {
     return false;
@@ -4531,7 +4532,7 @@ bool ssl_add_clienthello_tlsext(SSL_HANDSHAKE *hs, CBB *out, CBB *out_encoded,
     last_was_empty = (bytes_written == 4);
   }

-  if (ssl->ctx->grease_enabled) {
+  if (ssl->config->grease_enabled) {
     // Add a fake non-empty extension. See RFC 8701.
     if (!add_padding_extension(
             &extensions, ssl_get_grease_value(hs, ssl_grease_extension2), 1)) {
@@ -4544,9 +4545,25 @@ bool ssl_add_clienthello_tlsext(SSL_HANDSHAKE *hs, CBB *out, CBB *out_encoded,
   // intolerant to the last extension being zero-length. See
   // https://crbug.com/363583.
   bool offering_psk = should_offer_psk(hs, type);
-  if (!offering_psk && last_was_empty && !SSL_is_dtls(ssl) &&
-      !SSL_is_quic(ssl) && !ssl->s3->used_hello_retry_request) {
-    if (!add_padding_extension(&extensions, TLSEXT_TYPE_padding, 1)) {
+  size_t padding_len = ssl->config->client_hello_padding_length;
+  if (!SSL_is_dtls(ssl) && !SSL_is_quic(ssl) &&
+      !ssl->s3->used_hello_retry_request &&
+      (ssl->config->client_hello_padding_enabled || padding_len != 0)) {
+    if (padding_len == 0 && !offering_psk && last_was_empty) {
+      padding_len = 1;
+    }
+    const size_t msg_len = 2 /* version */ + CBB_len(out) + 2 /* extensions */ +
+                           CBB_len(&extensions);
+    if (padding_len == 0 && msg_len > 0xff && msg_len < 0x200) {
+      padding_len = 0x200 - msg_len;
+      if (padding_len >= 5) {
+        padding_len -= 4;
+      } else {
+        padding_len = 1;
+      }
+    }
+    if (padding_len != 0 &&
+        !add_padding_extension(&extensions, TLSEXT_TYPE_padding, padding_len)) {
       return false;
     }
   }
diff --git a/ssl/handshake_client.cc b/ssl/handshake_client.cc
index a4a53cda6..dbfb18a23 100644
--- a/ssl/handshake_client.cc
+++ b/ssl/handshake_client.cc
@@ -103,7 +103,7 @@ static bool ssl_write_client_cipher_list(const SSL_HANDSHAKE *hs, CBB *out,
   }

   // Add a fake cipher suite. See RFC 8701.
-  if (ssl->ctx->grease_enabled &&
+  if (ssl->config->grease_enabled &&
       !CBB_add_u16(&child, ssl_get_grease_value(hs, ssl_grease_cipher))) {
     return false;
   }
diff --git a/ssl/internal.h b/ssl/internal.h
index 96f6d3d3d..dcf77070d 100644
--- a/ssl/internal.h
+++ b/ssl/internal.h
@@ -918,7 +918,7 @@ struct NamedGroup {
 Span<const NamedGroup> NamedGroups();

 // kNumNamedGroups is the number of supported groups.
-constexpr size_t kNumNamedGroups = 6u;
+constexpr size_t kNumNamedGroups = 7u;

 // DefaultSupportedGroupIds returns the list of IDs for the default groups that
 // are supported when the caller hasn't explicitly configured supported groups.
@@ -3546,6 +3546,17 @@ struct SSL_CONFIG {
   // Enable signed certificate time stamps. Currently client only.
   bool signed_cert_timestamps_enabled : 1;

+  // Whether to GREASE the signature_algorithms ClientHello extension.
+  bool grease_sigalgs_enabled : 1;
+
+  // Whether GREASE (RFC 8701) is enabled for this connection.
+  bool grease_enabled : 1;
+
+  // Whether to add the compatibility padding extension to ClientHellos.
+  bool client_hello_padding_enabled : 1;
+
+  uint16_t client_hello_padding_length = 0;
+
   // ocsp_stapling_enabled is only used by client connections and indicates
   // whether OCSP stapling will be requested.
   bool ocsp_stapling_enabled : 1;
diff --git a/ssl/ssl_key_share.cc b/ssl/ssl_key_share.cc
index 84394b247..09cc72150 100644
--- a/ssl/ssl_key_share.cc
+++ b/ssl/ssl_key_share.cc
@@ -195,9 +195,11 @@ class X25519KeyShare : public SSLKeyShare {
 // RFC 10024
 class X25519MLKEM768KeyShare : public SSLKeyShare {
  public:
-  X25519MLKEM768KeyShare() {}
+  explicit X25519MLKEM768KeyShare(
+      uint16_t group_id = SSL_GROUP_X25519_MLKEM768)
+      : group_id_(group_id) {}

-  uint16_t GroupID() const override { return SSL_GROUP_X25519_MLKEM768; }
+  uint16_t GroupID() const override { return group_id_; }

   bool Generate(CBB *out) override {
     uint8_t mlkem_public_key[MLKEM768_PUBLIC_KEY_BYTES];
@@ -281,6 +283,7 @@ class X25519MLKEM768KeyShare : public SSLKeyShare {
   }

  private:
+  uint16_t group_id_;
   uint8_t x25519_private_key_[32];
   MLKEM768_private_key mlkem_private_key_;
 };
@@ -350,6 +353,7 @@ constexpr NamedGroup kNamedGroups[] = {
     {NID_secp384r1, SSL_GROUP_SECP384R1, "P-384", "secp384r1"},
     {NID_secp521r1, SSL_GROUP_SECP521R1, "P-521", "secp521r1"},
     {NID_X25519, SSL_GROUP_X25519, "X25519", "x25519"},
+    {NID_X25519MLKEM768, SSL_GROUP_X25519_KYBER768, "X25519Kyber768", ""},
     {NID_X25519MLKEM768, SSL_GROUP_X25519_MLKEM768, "X25519MLKEM768", ""},
     {NID_ML_KEM_1024, SSL_GROUP_MLKEM1024, "MLKEM1024", ""},
 };
@@ -383,6 +387,8 @@ UniquePtr<SSLKeyShare> SSLKeyShare::Create(uint16_t group_id) {
       return MakeUnique<X25519KeyShare>();
     case SSL_GROUP_X25519_MLKEM768:
       return MakeUnique<X25519MLKEM768KeyShare>();
+    case SSL_GROUP_X25519_KYBER768:
+      return MakeUnique<X25519MLKEM768KeyShare>(SSL_GROUP_X25519_KYBER768);
     case SSL_GROUP_MLKEM1024:
       return MakeUnique<MLKEM1024KeyShare>();
     default:
diff --git a/ssl/ssl_lib.cc b/ssl/ssl_lib.cc
index a0c746b38..fc314bece 100644
--- a/ssl/ssl_lib.cc
+++ b/ssl/ssl_lib.cc
@@ -526,6 +526,7 @@ SSL *SSL_new(SSL_CTX *ctx) {
   ssl->config->custom_verify_callback = ctx_impl->custom_verify_callback;
   ssl->config->retain_only_sha256_of_client_certs =
       ctx_impl->retain_only_sha256_of_client_certs;
+  ssl->config->grease_sigalgs_enabled = ctx_impl->grease_sigalgs_enabled;
   ssl->config->permute_extensions = ctx_impl->permute_extensions;
   ssl->config->aes_hw_override = ctx_impl->aes_hw_override;
   ssl->config->aes_hw_override_value = ctx_impl->aes_hw_override_value;
@@ -568,6 +569,7 @@ SSL *SSL_new(SSL_CTX *ctx) {

   ssl->config->signed_cert_timestamps_enabled =
       ctx_impl->signed_cert_timestamps_enabled;
+  ssl->config->grease_enabled = ctx_impl->grease_enabled;
   ssl->config->ocsp_stapling_enabled = ctx_impl->ocsp_stapling_enabled;
   ssl->config->handoff = ctx_impl->handoff;
   ssl->quic_method = ctx_impl->quic_method;
@@ -585,6 +587,9 @@ SSL_CONFIG::SSL_CONFIG(SSLImpl *ssl_arg)
       ech_grease_enabled(false),
       reject_unusable_ech_config(false),
       signed_cert_timestamps_enabled(false),
+      grease_sigalgs_enabled(false),
+      grease_enabled(false),
+      client_hello_padding_enabled(true),
       ocsp_stapling_enabled(false),
       channel_id_enabled(false),
       retain_only_sha256_of_client_certs(false),
@@ -3276,10 +3281,42 @@ void SSL_CTX_set_grease_enabled(SSL_CTX *ctx, int enabled) {
   FromOpaque(ctx)->grease_enabled = !!enabled;
 }

+void SSL_set_grease_enabled(SSL *ssl, int enabled) {
+  auto *ssl_impl = FromOpaque(ssl);
+  if (!ssl_impl->config) {
+    return;
+  }
+  ssl_impl->config->grease_enabled = !!enabled;
+}
+
 void SSL_CTX_set_grease_sigalgs_enabled(SSL_CTX *ctx, int enabled) {
   FromOpaque(ctx)->grease_sigalgs_enabled = !!enabled;
 }

+void SSL_set_grease_sigalgs_enabled(SSL *ssl, int enabled) {
+  auto *ssl_impl = FromOpaque(ssl);
+  if (!ssl_impl->config) {
+    return;
+  }
+  ssl_impl->config->grease_sigalgs_enabled = !!enabled;
+}
+
+void SSL_set_client_hello_padding_enabled(SSL *ssl, int enabled) {
+  auto *ssl_impl = FromOpaque(ssl);
+  if (!ssl_impl->config) {
+    return;
+  }
+  ssl_impl->config->client_hello_padding_enabled = !!enabled;
+}
+
+void SSL_set_client_hello_padding_length(SSL *ssl, uint16_t length) {
+  auto *ssl_impl = FromOpaque(ssl);
+  if (!ssl_impl->config) {
+    return;
+  }
+  ssl_impl->config->client_hello_padding_length = length;
+}
+
 void SSL_CTX_set_permute_extensions(SSL_CTX *ctx, int enabled) {
   FromOpaque(ctx)->permute_extensions = !!enabled;
 }
